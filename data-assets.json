{
  "what": "Derived payloads that are published with the site but not committed to the repository.",
  "why": "scripts/check_repo_size.py caps the tracked tree at 60 MB and says, when that trips, the fix is to stop committing derived payloads rather than to raise the ceiling — they are reproducible from the pipeline, so they belong in a release asset fetched at build time. This file is that list. Each entry names the release it comes from and the checksum it must have, so a commit fetches exactly the payload it was built against rather than whatever the tag points at today.",
  "howToAdd": "Build the file, then `uv run python scripts/publish_release_data.py --id <id>` to upload it and rewrite this manifest with the size and checksum of exactly what was uploaded. Commit the manifest. The deploy fetches by checksum, so a manifest that is not committed means the site keeps the previous payload.",
  "assets": [
    {
      "id": "transport-road-speeds",
      "tag": "data-v1",
      "asset": "road-speeds.geojson",
      "destination": "simulators/transport/data/road-speeds.geojson",
      "bytes": 5706336,
      "sha256": "4abc46261b90e5184badc4425ac0aabd1efa051e9a5f75ed2c3f001e680a14f3",
      "producedBy": "simulators/transport/scripts/export_speed_limits.py",
      "needs": "simulators/administrativ/data/raw/romania-latest.osm.pbf (327 MB, not committed)",
      "what": "Signed maxspeed per road portion over 76 653 km of motorway to tertiary, dissolved by value.",
      "withoutIt": "The transport map's speed-limit toggle disables itself and says why; nothing else changes and no build fails."
    },
    {
      "id": "administrativ-roads",
      "tag": "data-v1",
      "asset": "roads.geojson",
      "destination": "simulators/administrativ/web/public/data/roads.geojson",
      "bytes": 2468272,
      "sha256": "5ff19ac4251c2124ba744940ed55a5e40562d5eec1c00622961f07c09246d907",
      "producedBy": "simulators/administrativ/pipeline/export.py (write_roads)",
      "needs": "simulators/administrativ/data/raw/romania-latest.osm.pbf (327 MB, not committed)",
      "what": "Motorway, trunk and primary roads, dissolved by class and simplified to 300 m — map context for the administrative and transport maps.",
      "withoutIt": "The administrative map's road toggle disables itself and says so; the transport map's road toggles likewise. No build fails."
    },
    {
      "id": "administrativ-roads-county",
      "tag": "data-v1",
      "asset": "roads-county.geojson",
      "destination": "simulators/administrativ/web/public/data/roads-county.geojson",
      "bytes": 4043446,
      "sha256": "0a8f43f71e69e23f5f3bf7834359d38e51e58973ffd3155eec76b739b413cae7",
      "producedBy": "simulators/administrativ/pipeline/export.py (write_roads)",
      "needs": "simulators/administrativ/data/raw/romania-latest.osm.pbf (327 MB, not committed)",
      "what": "Secondary and tertiary roads, simplified to 150 m — the network the model routes most of its distances over.",
      "withoutIt": "The administrative map's road toggle disables itself and says so; the transport map's road toggles likewise. No build fails."
    }
  ]
}
